# ── Copy these into Railway's Environment Variables dashboard ──────────────────

# Groq API Key (https://console.groq.com)
GROQ_API_KEY=gsk_your_key_here

# JWT Secret — use a long random string (generate: openssl rand -hex 32)
JWT_SECRET=replace-with-long-random-string

# Google OAuth Client ID (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com

# Frontend URL — Railway will give you a URL like https://your-app.up.railway.app
FRONTEND_URL=https://your-app.up.railway.app

# Vite env var for Google OAuth (same Client ID)
VITE_GOOGLE_CLIENT_ID=your_client_id.apps.googleusercontent.com

# SQLite persistent volume mount path (set after adding a volume in Railway)
# RAILWAY_VOLUME_MOUNT_PATH=/data

# Node environment
NODE_ENV=production

# Port — Railway sets this automatically, leave as-is
# PORT=3001
